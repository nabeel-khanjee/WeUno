import 'package:flutter/material.dart';

const primaryColor = Color.fromARGB(255, 18, 124, 192);
const secondaryColor = Color.fromARGB(255, 106, 103, 116);
const darkColor = Color.fromARGB(255, 79, 79, 88);
const bodyTextColor = Color.fromARGB(255, 177, 177, 177);
const bgColor = Color.fromARGB(255, 237, 244, 248);
const canvasColor = Colors.white;
const defaultPadding = 20.0;
const defaultDuration = Duration(seconds: 1); // we use it on our animation
const maxWidth = 1440.0; // max width of our web
